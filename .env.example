# Root-level environment reference (copy to apps/api/.env for backend)

# API
PORT=3000
NODE_ENV=development

# Database (Phase 2)
# DATABASE_URL=postgresql://user:password@localhost:5432/wimm

# Auth (Phase 2)
# JWT_SECRET=changeme
# JWT_REFRESH_SECRET=changeme
# JWT_EXPIRES_IN=15m
# JWT_REFRESH_EXPIRES_IN=7d
